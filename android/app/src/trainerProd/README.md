# Flavor Configuration

Place your environment-specific `google-services.json` file in this folder.

Android will automatically select the file from this directory when building the specific flavor combination (e.g., `adminDev`).
